import { AppRegistry } from 'react-native';
// import App from './App';

// AppRegistry.registerComponent('callingapp', () => App);

import App from './App/App';

//to disable warnings
console.disableYellowBox = true;

AppRegistry.registerComponent('callingapp', () => App);
